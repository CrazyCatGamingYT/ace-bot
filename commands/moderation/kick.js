module.exports = {
    name : 'kick',
    timeout : 5000,
    description : 'this command kicks people but is currently not working cuz this bot is in early development',
    //run : async(client, message, args) => {
        //if(!message.guild.me.hasPermission('KICK_MEMBERS')) return message.channel.send('I dont have permission to kick people change it in my roles');
        //const Member = message.mentions.members.first()
        //if(!Member) return message.channel.send('Please specify who you would like to kick');
        //await Member.kick({ reason : args.slice(1).join(" ")})
        //message.channel.send(`${Member.user.tag} was kicked from the server!`)
    }