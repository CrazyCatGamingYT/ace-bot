module.exports = {
    name : 'ban',
    timeout : 5000,
    description : 'this command bans people but is currently not working cuz this bot is in early development',
    //run : async(client, message, args) => {
        //if(!message.member.hasPermission('BAN_MEMBERS')) return message.reply('You dont have permission to use this command')
        //if(!message.guild.me.hasPermission('BAN_MEMBERS')) return message.reply('I dont have permission to ban people change it in my roles');
        //const Member = message.mentions.members.first()
        //if(!Member) return message.reply('Please specify who you would like to ban');
        //await Member.ban({ reason : args.slice(1).join(" ")})
        //message.reply(`${Member.user.tag} was banned from the server!`)
    }